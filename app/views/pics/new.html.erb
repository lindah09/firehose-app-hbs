
<div class="smurfy text-left">

	<%= simple_form_for @pic do |f| %>
		<%= f.input :title %>
		<%= f.input :subtitle %>
		<%= f.input :message %>

		<%= f.submit "Save" %>

	<% end %>

</div>